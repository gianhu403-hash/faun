# Утренний отчёт — ночь 2026-06-19→20: Perch 2 на проде + первая видовая метрика

**Дата:** 2026-06-19 · **Ветка:** `feat/perch2-live` (от `main` `d80dc5f`) → смёржена в `main`.

## Вердикт (сначала вывод)

**Главная цель достигнута: `faun.antopkin.ru/` больше НЕ заглушка — сайт реально распознаёт
виды птиц через Perch 2.** Дополнительно впервые в проекте получена **честная видовая метрика**
(macro-F1 ≈ 0.80 / CV-accuracy 0.845 на реальных метках iNatSounds). Всё смёржено в `main`,
тесты зелёные (**525 passed / 2 skipped**), деплой проверен сквозь домен, демо v1 не задето,
откат готов.

---

## ⚠️ Что НЕ сделал (наверх — читать первым)

1. **Точность задеплоенного zero-shot Perch 2 на нашей фауне НЕ измерена.** Прод выдаёт реальные
   виды из 14795-классовой головы, но без дообучения; чтобы назвать «точность X%» на raw180,
   нужна человеческая разметка заповедника — её по-прежнему нет. Заказчику **не писать процент
   точности про прод**.
2. **Видовая метрика — на 12 видах (PoC), не на полной фауне заповедника.** Самые ранние в архиве
   iNatSounds виды Aves (хищники/водоплавающие + `Upupa epops`, `Apus apus`), смесь СА+ЕС.
3. **Обученная проба (`perch2_probe.pkl`, 12 видов) в прод НЕ выкачена — намеренно.** Заменять
   14795-классовый zero-shot 12-видовой пробой было бы регрессом. Прод = zero-shot Perch 2.
4. **Реальный fine-tune трансформера (PaSST) не гонял** — GPU 2060S занят живым демо v1; код run-ready.
5. **n held-out мал (84 клипа)** — CI на held-out ненадёжен (помечено); основное число — CV на 336.

---

## Что сделал · что именно · что принесло

| Что | Что именно | Что принесло |
|---|---|---|
| **Perch 2 на проде (главное)** | TF-образ `deploy/Dockerfile.ml` (tensorflow-cpu 2.20 + Perch 2 SavedModel в томе `faun-models`), blue-green перекат `faun-api:2.2.0-ml-20260619` на `100.64.0.1:8010` | Сайт реально распознаёт виды; тот же порт/nginx/auth |
| **Парсинг видовых меток** | `Perch2Adapter._load_labels()` читает `assets/labels.csv` (14795 имён, дроп заголовка), fail-safe против off-by-one | CSV/детекции — настоящие научные имена, не `species_<i>` |
| **Первая видовая метрика** | iNatSounds val → 12 видов × 35 клипов → эмбеддинги Perch 2 → проба → `species_eval(synthetic=False)` | **macro-F1 0.798, CV-acc 0.845 [0.822–0.869]**, `provenance=real-eval` |
| **Петля дообучения** | `faun export-labels` (детекции→CSV, абс. пути), `retrain --model perch/perch-v2`, `PerchProbeAdapter` | Операторские метки подключаются к дообучению без правок кода |

**Гейты деплоя (все зелёные до cutover):** TF-load (модель грузится, 14795 меток, embedding 1536) →
green-job на `:8011` (реальный HTTP-job → реальные виды `model:perch-v2`) → cutover → проверка сквозь
домен (`/`=401 без / 200 с auth, `/v1/`=200 демо живо, `/healthz` version=тег). Реальные виды на проде:
`Botaurus stellaris`, `Caprimulgus europaeus`, `Strix aluco`…

---

## Проблемы и баги (встретил → починил)

- **TF wheel под Python 3.12** — проверил: `tensorflow-cpu 2.20.0` имеет cp312-колесо → база `python:3.12-slim` без даунгрейда.
- **iNatSounds val = 26.6 ГБ, S3 ~5 МБ/с single-stream** → распараллелил 16 range-загрузок (~40 МБ/с).
- **tar НЕ отсортирован** (начинается с prefix 01085) → ранний `break` извлёк 0; убрал, полный скан.
- **`ModuleNotFoundError: faun`** в метрик-контейнере → `PYTHONPATH=/app`; права на клипы → `--user 1001`.
- **Аудит (5 ревьюеров → голосование 2/3 → completeness-critic):** 0/13 подтверждено голосованием,
  critic нашёл 2 реальных — **починил**: (а) off-by-one в метках → fail-safe (сентинел-заголовок +
  cross-check `len(labels)==len(logits)`); (б) разрыв петли `export-labels→retrain` → абсолютный `segment_path` + e2e-тест.

---

## Состояние кода и артефакты

- Ветка `feat/perch2-live`: P1-build `8066916`, P2-build `74c420f`, audit-fix `5b528d9`, docs — смёржено в `main` (`--no-ff`), push под gianhu403-hash.
- **525 passed / 2 skipped**; bandit 0 HIGH; module-level TF/torch — нет; frozen-сигнатуры целы; секрет-скан диффа чист.
- Кластер: прод `faun-api:2.2.0-ml-20260619` (perch-v2, auth ON), откат `faun-api:2.1.0-20260619` (stub);
  модель — том `faun-models:/models/perch2` (392 МБ); данные `/home/oleg/faun-data/datasets/inatsounds/`
  (val.tar.gz 26.6 ГБ + `birdsubset/` 12 видов); проба `perch2_probe.pkl` (146 КБ, PoC).

---

## Векторы дальше

1. **Расширить видовую метрику**: больше видов (включая фауну заповедника РФ) и клипов, тем же путём.
2. **Точность zero-shot Perch 2 на raw180** — нужна человеческая разметка (review-UI + `export-labels` готовы).
3. **(Опц.) Проба в прод** — когда покроет осмысленный видовой набор и пройдёт health-gate (`FAUN_CLASSIFIER=perch-probe` + `PERCH_V2_PROBE_PATH`).
4. **Fine-tune PaSST** — при свободном GPU-окне.
5. **Пересборка прод-образа из merged-main** — косметика провенанса (P2-код аддитивный/ленивый, в zero-shot не задействован), не требуется для корректности.
