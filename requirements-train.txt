# =============================================================================
# requirements-train.txt — ТОЛЬКО для РЕАЛЬНОГО fine-tune аудио-трансформера
# (faun.training) на cluster-alex, образ faun-ml-torch (GPU). НЕ ставится в CI и
# НЕ нужен для control-flow тестов (они гоняются на numpy-стабе, torch-free).
#
# Базовый pipeline-стек — requirements-pipeline.txt (тяжёлый ML там lazy). Этот
# файл аддитивен: ставится поверх, только на машине, где реально гоняем дообуч.
#
# Установка (на кластере):
#   pip install -r requirements-pipeline.txt -r requirements-train.txt
# =============================================================================

# --- PyTorch стек (cluster-only; в CI / локальном control-flow НЕ ставится) ---
# Кластер cluster-alex ставит с индекса cu130 (CUDA 13). ПРОВЕРЕНО на кластере
# 2026-06-19: cu130-колёса torch — 2.9.0..2.12.1 (УСТАНОВЛЕНО 2.12.0+cu130);
# прежний потолок <2.7 был НЕустановим на cu130 (колёс ниже 2.9 там нет).
# Floor >=2.9 заодно закрывает CVE-2025-32434 (RCE в torch.load, патч 2.6.0),
# затрагивавший faun.training.load_checkpoint/resume; 2.12 на кластере уже патчен.
torch>=2.9,<2.13
# torchaudio: cu130-колёса 2.9.0..2.11.0; в образе faun-ml-torch НЕ установлен,
# fine-tune (PaSST) его не требует (свой mel-frontend). Держим аддитивно в
# cu130-совместимом диапазоне; ставить только если конкретный бэкбон попросит.
torchaudio>=2.9,<2.12

# --- PaSST (продуктовый дефолт-бэкбон): код Apache-2.0; нативно 32 кГц ---------
#   from hear21passt.base import get_basic_model, get_model_passt
hear21passt>=0.0.26
timm>=0.9            # PaSST зависит от timm (patch-embed / vit-утилиты)

# --- Альтернативные бэкбоны (подключаемые; см. docs/finetuning.md) ------------
#   AST  — HF transformers (MIT/ast-finetuned-audioset-10-10-0.4593), код BSD-3
#   BEATs — код MIT, веса под кастомной MS-лицензией (проверять перед коммерцией)
transformers>=4.40   # AST backbone (опционально)

# --- Утилиты обучения ----------------------------------------------------------
#   class-weight/метрики переиспользуют scikit-learn из requirements-pipeline.txt
#   (StratifiedKFold/balanced — уже в базовом стеке).
