"""Bundled data assets for faun (non-code resources shipped with the package).

Currently holds ``reserve_checklist.txt`` — the default *example* regional
species allow-list seed (ADR-0004); a placeholder list, NOT a validated
reserve checklist. Files here are resolved by path, not imported.
"""
