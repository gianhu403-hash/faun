"""Bundled data assets for faun (non-code resources shipped with the package).

Currently holds ``reserve_checklist.txt`` — the default regional species
allow-list (ADR-0004). Files here are resolved by path, not imported.
"""
