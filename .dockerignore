# Version control
.git/
.gitignore

# Python artifacts
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/

# ML models (loaded via scp, not in image)
*.keras
*.h5
*.npz

# Tests & docs (not needed in production image)
tests/
docs/
graphs/

# Demo data — runtime, not needed in image (provided via bind-mount in compose)
demo/

# Local-only tooling
simulator/
tasks/
site/
*.pdf
requirements.lock
mkdocs.yml
cleanup.sh
demo.sh
generate_presentation_graphs.py

# Claude / MCP metadata
.claude/
.mcp.json

# IDE / OS
.vscode/
.idea/
.DS_Store

# Environment
.env
.env.*
