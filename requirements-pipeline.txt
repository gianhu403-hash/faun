# Faun v2 batch-pipeline (TF-free). Pinned to exact resolved versions for
# reproducible deploy images; heavy ML libs (TF/torch) stay lazy, not listed here.
fastapi==0.135.1
uvicorn[standard]==0.42.0
python-multipart==0.0.31
numpy==2.2.6
soundfile==0.13.1
soxr==1.0.0
pydantic==2.12.5
aiofiles==25.1.0
httpx==0.28.1
scipy==1.14.1
scikit-learn==1.5.0
# Spectrogram PNG rendering for the review UI (FR-008); Agg backend, imported
# lazily in faun/spectrogram.py so it never loads at import time.
matplotlib==3.9.2
